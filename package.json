{
  "name": "voltage-ecommerce",
  "private": true,
  "scripts": {
    "seed:mongo": "npm --prefix server run seed:mongo",
    "server": "npm --prefix server run dev",
    "client": "npm --prefix client run dev"
  },
  "dependencies": {
    "mongodb": "^7.6.0"
  }
}
